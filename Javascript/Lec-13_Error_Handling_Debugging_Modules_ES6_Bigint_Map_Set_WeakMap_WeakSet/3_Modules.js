/****
 *   what is module?
 *     -> A way to make your code more modular, maintainable and scalable.
 *     -> Can reuse such modular code anywhere in Different JS files.
 * 
 *     -> In ES6 ->  2 keywords were introduced "export" and "import"

 */

import { add } from './utils/mathUtil.js';

console.log(add(2,3));